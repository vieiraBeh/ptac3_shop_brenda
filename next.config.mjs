/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "cdn-icons-png.freepik.com"
        ]
    }
};

export default nextConfig;
