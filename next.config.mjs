/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "ead.ifms.edu.br"
        ]
    }
};

export default nextConfig;
